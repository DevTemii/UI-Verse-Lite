# Usage

Open `index.html` in your browser to see demos of components.
Copy the HTML from `components/*.html` into your project and include `assets/css/styles.css`.

Example:
```html
<link rel="stylesheet" href="assets/css/styles.css">
<!-- paste component -->
```
